[Frontend User]
      |
      | 1. Request pre-signed multipart upload URL
      v
[Backend Node.js API]
      |
      | 2. Generate multipart pre-signed URLs
      v
[S3 Private Bucket: uploads/]
      ^
      | 3. Frontend uploads .blend file directly (multipart)
      |
[Frontend]
      |
      | 4. Notify backend → Store S3 key & job metadata in DB
      v
[DB]
      |
      | 5. Add job to Job Queue / Assign Renderer
      v
[Renderer Node Provider]
      |
      | 6. Get pre-signed GET URL → Download .blend securely
      | 7. Render frames locally
      | 8. Upload frames securely to S3 (renders/job-123/)
      v
[S3 Private Bucket: renders/]
      |
      | 9. CloudFront + OAC serves frames to users
      v
[Frontend / End User]
      | 10. View frames one by one (lazy load)
      | 11. Download ZIP (pre-generated or streamed via backend)

